# Debian URL 解析器 - 快速参考

## 🚀 快速开始

```python
from debian_url_parser import DebianURLParser

parser = DebianURLParser()
info = parser.parse_url('你的URL')

# 基本信息
print(f"包名: {info.package_name}")
print(f"版本: {info.version}")
print(f"架构: {info.architecture}")
print(f"PURL: {info.package_purl}")
```

## 📋 支持的文件格式

| 文件格式 | 说明 | 示例 |
|---------|------|------|
| `.deb` | 标准二进制包 | `nginx_1.18.0-6.1_amd64.deb` |
| `.udeb` | 安装器微型包 | `espeak-ng-data-udeb_1.49.2+dfsg-6_amd64.udeb` |
| `.dsc` | 构建描述文件 | `nginx_1.28.0-6.dsc` |
| `.orig.tar.gz` | 上游源码 | `nginx_1.28.0.orig.tar.gz` |
| `.orig.tar.xz` | 上游源码(xz压缩) | `nginx_1.28.0.orig.tar.xz` |
| `.orig.tar.gz.asc` | 上游源码签名 | `nginx_1.28.0.orig.tar.gz.asc` |
| `.debian.tar.gz` | Debian补丁(现代) | `nginx_1.28.0-6.debian.tar.gz` |
| `.debian.tar.xz` | Debian补丁(现代,xz) | `nginx_1.28.0-6.debian.tar.xz` |
| `.diff.gz` | Debian补丁(老式) | `djbdns_1.05-4.diff.gz` |
| `.tar.gz` | 自研组件源码 | `dpkg_1.23.3.tar.xz` |

## 🏷️ 版本号格式

### 标准格式
```
[epoch:]upstream_version[-debian_revision]
```

### 示例
| 版本号 | epoch | upstream_version | debian_revision |
|--------|-------|------------------|-----------------|
| `1.18.0-6.1` | - | `1.18.0` | `6.1` |
| `2:5.10.0-1` | `2` | `5.10.0` | `1` |
| `1.23.3` | - | `1.23.3` | - (自研) |

## 🎯 特殊标识

| 标识 | 说明 | 示例 | 提取字段 |
|------|------|------|---------|
| `+dfsg` | DFSG重打包 | `1.49.2+dfsg-6` | `dfsg_marker` |
| `+really` | 版本降级 | `1.0+really1.0-1` | `really_version` |
| `+b2` | 二进制重制 | `1.0-1+b2` | `binary_rebuild` |
| `~bpo11+1` | Backports | `2.0-1~bpo11+1` | `backport_marker` |
| `+nmu1` | 非维护者上传 | `1.0-1+nmu1` | `nmu_marker` |
| `ubuntu1` | Ubuntu版本 | `1.0-1ubuntu1.1` | `ubuntu_marker` |
| `~exp1` | 实验性版本 | `1.05-9~exp1` | `exp_marker` |
| `+deb12u1` | 安全更新 | `1.0-1+deb12u1` | `release_version`, `security_update` |

## 📦 DebianPackageInfo 字段

### 基本信息
```python
info.url                  # 原始URL
info.filename             # 文件名
info.package_name         # 包名
info.version              # 完整版本(含架构)
info.architecture         # 架构(二进制包)
info.package_purl         # Package URL
```

### 版本信息
```python
info.epoch                # 纪元号
info.upstream_version     # 上游版本
info.debian_revision      # Debian修订号
```

### 特殊标识
```python
info.dfsg_marker          # DFSG标识
info.really_version       # Really版本
info.binary_rebuild       # 二进制重制
info.backport_marker      # Backports标识
info.nmu_marker           # NMU标识
info.ubuntu_marker        # Ubuntu标识
info.exp_marker           # 实验性标识
info.release_version      # 发行版版本
info.security_update      # 安全更新
```

### 分类信息
```python
info.distribution_type    # 分发类型: source | binary
info.file_type            # 文件类型: binary | upstream_source | upstream_source_patch | native_source | build_file
info.is_native            # 是否为自研组件
```

### 便捷属性
```python
info.is_dfsg              # 是否DFSG重打包
info.dfsg_version         # DFSG版本号
info.is_really            # 是否Really降级
info.is_prerelease        # 是否预发布版本
info.is_binary_rebuild    # 是否二进制重制
info.binary_rebuild_version  # 二进制重制版本号
info.is_backport          # 是否Backport
info.backport_release     # Backport目标发行版
info.backport_version     # Backport版本号
info.is_nmu               # 是否NMU
info.nmu_version          # NMU版本号
info.is_ubuntu            # 是否Ubuntu版本
info.ubuntu_version       # Ubuntu版本号
info.release_version_number  # 发行版版本号(纯数字)
```

## 🔍 使用场景

### 场景1: 解析现代Debian包
```python
url = 'https://snapshot.debian.org/archive/debian/20251220T083035Z/pool/main/d/dpkg/dpkg_1.23.3_armhf.deb'
info = parser.parse_url(url)
# 包名: dpkg, 版本: 1.23.3_armhf, 架构: armhf
```

### 场景2: 解析老式包(Debian 2.x)
```python
url = 'https://snapshot.debian.org/archive/debian-archive/20090802T004153Z/debian/dists/slink/contrib/binary-i386/misc/dbf2mysqL_1.10b-2.deb'
info = parser.parse_url(url)
# 包名: dbf2mysqL, 架构: i386 (从路径提取)
```

### 场景3: 解析老式补丁
```python
url = 'https://snapshot.debian.org/archive/debian/20080815T000000Z/pool/main/d/djbdns/djbdns_1.05-4.diff.gz'
info = parser.parse_url(url)
# 文件类型: upstream_source_patch
```

### 场景4: 解析Backport包
```python
url = 'https://snapshot.debian.org/archive/debian-archive/20110127T084257Z/debian-backports/pool/main/d/djbdns/djbdns_1.05-2~bpo40+1.diff.gz'
info = parser.parse_url(url)
# Backport标识: bpo40+1, 目标发行版: 40 (Debian 4.0)
```

### 场景5: 解析安装器包
```python
url = 'https://snapshot.debian.org/archive/debian/20181024T025940Z/pool/main/e/espeak-ng/espeak-ng-data-udeb_1.49.2+dfsg-6_amd64.udeb'
info = parser.parse_url(url)
# 文件类型: binary, DFSG: dfsg
```

### 场景6: 导出为CSV/JSON
```python
from debian_url_parser import export_to_csv, export_to_json

package_infos = [parser.parse_url(url) for url in urls]

# 导出CSV
export_to_csv(package_infos, 'output.csv')

# 导出JSON
export_to_json(package_infos, 'output.json', pretty=True)
```

## 🧪 运行测试

```bash
# 特殊场景测试(17个用例)
python3 test_special_cases.py

# 完整测试套件(32个用例)
python3 test_complete_suite.py

# 所有测试
python3 test_special_cases.py && python3 test_complete_suite.py
```

## 📊 支持的Debian版本

| 版本 | 代号 | 年份 | 状态 |
|------|------|------|------|
| 2.0 | hamm | 1998 | ✅ |
| 2.1 | slink | 1999 | ✅ |
| 2.2 | potato | 2000 | ✅ |
| 3.x | woody/sarge/etch | 2002-2007 | ✅ |
| 4.x | etch | 2007-2010 | ✅ |
| 5.x | lenny | 2009-2012 | ✅ |
| 6.x+ | squeeze及更新 | 2011-至今 | ✅ |

## ❓ 常见问题

### Q1: 如何判断是否为自研组件?
```python
if info.is_native:
    print("这是Debian自研组件")
else:
    print("这是上游组件")
```

### Q2: 如何获取纯净的上游版本号?
```python
# upstream_version 已经是纯净版本,移除了所有特殊标识
print(info.upstream_version)  # 例如: 1.18.0
```

### Q3: 如何判断是否为安全更新?
```python
if info.security_update:
    print(f"这是安全更新 {info.security_update}")
    print(f"目标发行版: {info.release_version}")
```

### Q4: 老式包如何提取架构?
```python
# 脚本会自动从URL路径中提取架构信息
# 如果路径中有 binary-i386, 则 info.architecture = 'i386'
# 如果都没有, 则 info.architecture = 'unknown'
```

## 📚 相关文档

- `IMPROVEMENTS.md` - 详细改进说明
- `改进总结.md` - 改进总结
- `快速参考.md` - 本文档
- `test_special_cases.py` - 特殊场景测试
- `test_complete_suite.py` - 完整测试套件

## 🎯 核心改进

1. ✅ 支持 `.diff.gz` 老式补丁格式
2. ✅ 支持 `.udeb` 安装器包
3. ✅ 支持包名中的大写字母
4. ✅ 支持从URL路径提取架构信息
5. ✅ 100%向后兼容
6. ✅ 支持1998年至今的所有Debian版本

## 📞 联系方式

如有问题或建议,请查看项目文档或提交Issue。
